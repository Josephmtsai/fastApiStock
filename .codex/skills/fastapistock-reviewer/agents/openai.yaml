interface:
  display_name: "FastApiStock Reviewer"
  short_description: "Review changed files before QA."
  default_prompt: "Use $fastapistock-reviewer to review these changed files before QA."
policy:
  allow_implicit_invocation: true
