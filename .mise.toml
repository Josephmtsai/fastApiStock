[tools]
# Use ubi backend to bypass aqua artifact attestation requirement.
# aqua:astral-sh/uv requires GitHub attestations that are not yet registered
# for newer uv releases, causing mise install to fail on Railway/Railpack.
"ubi:astral-sh/uv" = "0.11.9"
