[tools]
# Use github backend (replaces deprecated ubi) to bypass aqua attestation
# requirement. aqua:astral-sh/uv requires GitHub attestations not yet present
# for newer uv releases. Railway deployment uses Dockerfile instead of mise.
"github:astral-sh/uv" = "0.11.9"
